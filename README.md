# Next + TypeScript + Tailwind CSS + Material UI (starter)

Quick start:

1. Install dependencies

```bash
cd frontend
npm install
```

2. Run dev server

```bash
npm run dev
```

Lint:

```bash
npm run lint
```

Type-check:

```bash
npm run type-check
```
